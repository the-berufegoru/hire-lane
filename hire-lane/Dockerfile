# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build hire-lane`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t hire-lane`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system hire-lane && \
          adduser --system -G hire-lane hire-lane

COPY dist/hire-lane hire-lane/
RUN chown -R hire-lane:hire-lane .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix hire-lane --omit=dev -f install

CMD [ "node", "hire-lane" ]
